---
import LandingPage from '../../components/templates/LandingPage.astro';
import { landingPages } from '../../data/landing-pages';
const lp = landingPages.find((p) => p.slug === 'kleine-rate-grosser-sachwert');
if (!lp) throw new Error('missing lp');
---
<LandingPage lp={lp} />
